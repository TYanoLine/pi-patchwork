{
  "name": "pi-patchwork",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "test": "vitest run",
    "check": "npm run test && npm run build",
    "prepare:pi": "node scripts/generate-pi-assets.mjs",
    "predev": "npm run prepare:pi",
    "prebuild": "npm run prepare:pi"
  },
  "dependencies": {
    "@jsquash/webp": "^1.5.0",
    "@vitejs/plugin-react": "latest",
    "lucide-react": "latest",
    "react": "latest",
    "react-dom": "latest",
    "typescript": "latest",
    "vite": "latest"
  },
  "devDependencies": {
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "vitest": "latest"
  }
}
