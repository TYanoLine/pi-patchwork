# Pi Patchwork — Plan & Tasks

作成日: 2026-09-22 / 状態: 設計案 v0.1（実装・公開前）

## 目的と今回の範囲
任意の入力画像を、共有した円周率の数字列への参照と少数の補正値から、パッチワーク状に不可逆再現する。指定したサイズ予算内で再現誤差を小さくするブラウザアプリを作り、最終的に新規 GitHub リポジトリと Vercel の公開 URL を提供する。
今回は plan と tasks を確定可能な粒度にする。リポジトリ作成、コード実装、デプロイは未実施。
仮称・リポジトリ名: `pi-patchwork`。初期リポジトリは private を想定。所有アカウント、名称の空き、Vercel のチーム・接続権限は作成段階で確認する。

## 設計上の前提
- 円周率は画像そのものを含むという保証には頼らず、有限の共通辞書として使う。
- 大きなパッチをランダムに見える数字列だけで近似するのは難しい。JPEG/WebP より高効率になるとは仮定しない。まず実験で品質と計算量を評価する。
- 「圧縮率」と「探索時間」は別の制御軸。保存量を増やさず探索量を増やして品質改善できる場合はあるが、改善は保証されない。
- サイズ予算内の符号化は保証対象にできるが、任意画像の最低画質は保証しない。
- 元画像はブラウザ内で処理する。最初は PNG/JPEG/WebP の静止画、透明部分はユーザー指定の背景色へ合成。アニメーション、可逆圧縮、アルファ保持は対象外。

## 再現方式
### 共通辞書
円周率の小数部分の先頭 N=1,000,000 桁を共有辞書として使う。整数部の 3 を含めず、最初の 1 を offset=0 と定義する。辞書はビルド時に Chudnovsky 法で生成し、既知の SHA-256 と先頭桁で検証する。辞書 ID=1、桁数=1,000,000 を現行フォーマットの固定条件とし、異なる辞書での復号を拒否する。
探索用インデックスもビルド時に再生成する。2×2 / 4×4 / 8×8 / 16×16 の各開始位置を12bitの相対形状特徴で4096バケットへ分類し、各バケットに最大8個の代表 offset を保持する。インデックスは約512 KiBで、画像ごとの .pipw には含めない。

### パッチのレコード
| 項目 | 意味 |
| --- | --- |
| offset | 円周率の何桁目から読むか。整数の開始位置 |
| length / shape | 使用桁数。v4 では 2×2 / 4×4 / 8×8 / 16×16 の格子で 4 / 16 / 64 / 256 桁 |
| transform | 正方形格子の回転・反転、最大8種類 |
| gain | 模様の強さ。量子化した RGB 各チャンネルの係数 |
| bias | 平均色・明るさ。量子化した RGB 各チャンネルの加算値 |
| patch size | 描画領域の大きさ。分割木から導出し重複保存しない |

数字 d を q=2d−9 に変換し、shape の格子へ行優先に配置する。transform を適用し、パッチ領域へ固定仕様の双線形補間で拡大する。各チャンネル c を以下で復元する。

`pixel_c = clamp(round(gain_c × q + bias_c), 0, 255)`

初期案では RGB に同じ模様を使い色補正のみ分ける。色の境界が弱い場合は、後続版でチャンネル別 offset を比較評価する。座標の取り方、丸め、境界処理、gain の固定小数点精度をフォーマット仕様に記載し、同一ファイルから常に同じ画素を復元する。

gain=0 は単色パッチとなる。単色専用の短いレコードを設け、円周率パッチと比較する。これは単色部分を安く保存するための明示的なモードであり、UI で使用割合を表示する。

### 可変分割と探索
1. 画像全体を2の累乗の正方形にエッジ延長し、四分木で分割する。表示時は元の幅・高さで切り抜く。
2. 各領域について、単色と円周率候補を比較する。評価は表示範囲の RGB MSE を初期指標とする。
3. 4×4色特徴の主成分から12bitの相対形状ハッシュを作り、100万桁のπ特徴インデックスから近い断片を取得する。少数の均等サンプルもフォールバックとして混ぜ、候補ごとに gain/bias を最小二乗で推定する。
4. 量子化・クリップ後に再評価し、実際に復号される画素の誤差で順位を決める。
5. パッチ分割、使用桁数変更、補正精度変更それぞれの「誤差減少 / 追加ビット数」を比較し、予算内で改善する。
6. 最後に実際にシリアライズする。予算超過なら改善を取り消して再符号化する。

本番探索は100万桁を単純総当たりせず、特徴インデックスから得た候補と少数の均等サンプルを評価する。「最も近い」とは断定せず「探索した候補の中で最良」と扱う。Fast / Balanced / Thorough で参照バケット数・各バケットの候補数・再評価数を変える。
探索計算量の目安は O(パッチ数 × 候補数 × パッチ画素数)。辞書全体の走査はビルド時のインデックス生成へ移し、ブラウザ実行時は Web Worker 内の索引検索と再評価に限定する。

## 圧縮率とサイズ予算
主指定は「非圧縮RGBの何%を保存するか」。保存率 s、幅 W、高さ H に対し:

`rawBytes = W × H × 3`
`budgetBytes = floor(rawBytes × s / 100)`

例: 512×512 RGB は786,432 bytes。保存率10%なら78,643 bytes以下、削減率90%、約10:1。
入力 JPEG/PNG 自体のファイルサイズとの比は別表示にし、混同しない。ユーザーが希望する場合は目標 KB の指定にも対応する。

予算にはヘッダー、分割情報、パッチ参照、補正値、パディングをすべて含める。共有辞書を除いた `.pipw` サイズと、初回の辞書ダウンロード量を別々に表示する。自己完結パッケージのサイズを比較する場合には辞書も加算する。
最低ヘッダー＋単色レコードより小さい指定は達成不能として最小サイズを表示する。予算を満たすために無断で画像解像度を落とさない。低予算では全体が単色に近づく可能性をプレビューで示す。

実装上は D + λR による候補比較を利用できるが、MVP は限界改善量による貪欲割り当てと実バイト検証を優先する。大域最適は保証しない。探索モードは Fast / Balanced / Thorough とし、同じサイズ予算で探索努力だけを変える。

## ファイル形式 .pipw v4
- ヘッダー: magic、version、幅・高さ、色・背景設定、辞書ID/ハッシュ、辞書桁数、符号化プロファイル。
- 本体: 四分木の分割フラグと、葉ごとの単色または円周率参照レコード。深さ優先の固定順で座標を復元する。
- JSON はデバッグ用。ダウンロードはビットパックしたバイナリを正とする。v4 は描画パッチサイズと内部ソース格子を分離し、2×2 / 4×4 / 8×8 / 16×16 を2ビットで選択する。レコード長は12 bytes。旧フォーマットは読み込まず、version=4 / dictionary ID=1 のみ受理する。
- offset は32bitで保持する。現行100万桁では約20bit相当を使用し、使用格子が辞書末尾を越える候補は無効。将来辞書を変える場合は dictionary ID を更新し、互換を持たせず形式を明示的に更新する。
- パラメータのビット幅、エンディアン、丸め、未知バージョン処理は codec 実装前に確定する。
- デコーダは元画像なしで動作し、辞書不足・破損・範囲外参照・過大寸法を検証する。

## UI と構成
React + TypeScript + Vite、Canvas、Web Worker を初期構成にする。静的アプリとして Vercel へデプロイする。画像処理用のサーバーや永続DBは MVP に不要。

画面の流れ: 画像投入 → 保存率と探索モード → 処理・中断 → 原画像/復元画像の比較 → `.pipw` または復元 PNG の保存。
パッチ境界の表示を切り替え、選択パッチの開始桁、使用桁数、gain/bias、誤差を表示する。辞書ロードと処理の進捗を分ける。
結果には実バイト数、実現保存率、元ファイル比、PSNR/SSIM、処理時間、辞書コストを表示する。PNG書き出しサイズは独自圧縮形式のサイズとは別。
初期処理上限は512×512を仮設定とし、超える入力は縮小後の寸法・圧縮率基準を表示してユーザーが選べるようにする。実測後に上限を調整する。

## 検証と判断基準
- 写真・イラスト・文字・グラデーション・ノイズ・単色、非正方形・奇数寸法を評価する。
- 5/10/20/50% の保存率で、サイズ、品質、時間、メモリを記録する。
- 単色パッチのみ、固定seed疑似乱数辞書、JPEG/WebP と同じ実サイズ付近で比較する。汎用コーデックに負けても値を公開する。
- 円周率パッチが単色方式からどれだけ改善するかを分離評価する。π固有の優位性は前提にしない。
- encode→decode と独立ロードの画素一致、予算遵守、壊れた入力の拒否、処理中断を必須テストにする。
- 品質の数値目標は実測後に設定する。ブラウザ性能は機種とブラウザを併記する。
- 遅すぎる場合は候補数・インデックスを調整し、WASM/WebGPU は必要性が確認できた後の拡張とする。

## Tasks
| ID | 優先度 | 作業 | 依存 | 完了条件 |
| --- | --- | --- | --- | --- |
| T01 | P0 | 保存率・式・辞書・MVP範囲を設計に固定 | — | 本書の初期仕様を採用し変更点を記録 |
| T02 | P0 | 数値プロトタイプと評価画像を用意 | T01 | 小さな全探索例と単色ベースラインを比較できる |
| T03 | P0 | 辞書生成・検証・計算量ベンチマーク | T02 | ハッシュ・生成手順・品質と時間の計測表がある |
| T04 | P0 | 実験結果で手法を見直す | T03 | 継続する設定と残る品質限界を明記 |
| T05 | P0 | GitHub新規privateリポジトリ作成 | T04 | 所有先・名前を確定、READMEとplan/tasksを初回commit |
| T06 | P0 | Vite/React/TSとCIを構築 | T05 | 型検査・codecテスト・本番buildがCIで成功 |
| T07 | P0 | .pipw v1仕様と独立decoder実装 | T06 | 固定fixtureを元画像なしで正確に復号 |
| T08 | P0 | 固定パッチencoderとWorker化 | T07 | 円周率の候補照合・補正・中断が動作 |
| T09 | P0 | 可変分割とサイズ予算制御 | T08 | 達成可能な指定で全ファイルサイズが予算以下 |
| T10 | P0 | 比較UI・パッチ詳細・保存/読み込み | T09 | ブラウザを開き直して.pipwから復元可能 |
| T11 | P0 | 品質・性能・境界条件の検証 | T10 | 比較表、処理上限、既知の制約が文書化 |
| T12 | P0 | Vercel接続とPreviewデプロイ | T11 | build成功、辞書/Workerが配信され一連の操作が通る |
| T13 | P0 | Production公開とREADME更新 | T12 | 公開URL、リポジトリURL、再デプロイ手順を提供 |
| T14 | P1 | 色チャンネル別参照・高速化を検討 | T11 | 追加バイトと計算量に対する改善を比較 |

T01〜T12のMVPを実装・検証済み。辞書は100万桁へ拡張し、特徴インデックス検索、可変内部ソース格子、旧形式拒否を実装した。CIでは辞書/インデックス生成を含む test + production build が成功している。Vercel Preview をPRブランチで公開中。品質・速度の数値は100万桁版で再ベンチマークして更新する。

## 参考
- Vite の静的デプロイ: https://vite.dev/guide/static-deploy
- Vercel の Vite 対応: https://vercel.com/docs/frameworks/frontend/vite
- 円周率の数字統計に関する研究（統計的な一致は任意列の出現の証明ではない）: https://arxiv.org/abs/1612.00489
